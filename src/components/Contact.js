import React, { Component } from 'react';

import Cycle from "../boots/Cycle";

export default class Contact extends Component {
    render() {
        return (
            <div>
                <Cycle />
            </div>
        )
    }
}
